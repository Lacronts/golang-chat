const NAMESPACE = 'CONNECTION';

const ConnectionActionTypes = {
  OPEN_CONNECTION: `${NAMESPACE}_OPEN_CONNECTION`,
  CLOSE_CONNECTION: `${NAMESPACE}_CLOSE_CONNECTION`,
};

export { ConnectionActionTypes };
